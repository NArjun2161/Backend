package com.istato.admin.repository;

import com.istato.admin.model.ExecutiveApplication;

public interface ExecutiveApplicationRepo {
    ExecutiveApplication saveExecutiveApplication(ExecutiveApplication executiveApplication);
}
